# Copy to .env (gitignored). Use a random local test password only.
# Never commit real passwords. Never use production credentials.

POSTGRES_USER=kwzy_party_test
POSTGRES_PASSWORD=change-me-local-test-only
POSTGRES_DB=kwzy_party_test
POSTGRES_PORT=55432

# SQLAlchemy URL for tests / alembic (psycopg3)
# TEST_DATABASE_URL=postgresql+psycopg://kwzy_party_test:change-me-local-test-only@127.0.0.1:55432/kwzy_party_test
